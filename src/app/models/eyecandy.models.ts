
export class Guisante {
  id: number;
  posicion: number;
  dispara: boolean;

  constructor (id:number, posicion: number, dispara:boolean){
    this.id = id;
    this.posicion = posicion;
    this.dispara = dispara;

  }
}